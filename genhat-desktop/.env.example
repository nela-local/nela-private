# # NELA Cloud endpoints (desktop client only — no provider secrets here)
# #
# # Defaults (no .env needed for production):
# #   API  https://nela-gateway.onrender.com
# #   Web  https://nela-webpage.vercel.app
# # If production is unreachable, the app falls back to localhost.
# #
# # Local development override (optional):
# NELA_CLOUD_API_BASE_URL=http://localhost:3001
# NELA_CLOUD_WEB_BASE_URL=http://localhost:3000
#
# NELA ops / local only — end users never set this.
# Desktop OAuth client (not the website login client). Google still requires
# the Desktop "client secret" on the token request (it is not kept confidential).
# NELA_GOOGLE_CONNECTOR_CLIENT_ID=
# NELA_GOOGLE_CONNECTOR_CLIENT_SECRET=
#
# Telegram user connector (from https://my.telegram.org — NELA ops / local only).
# NELA_TELEGRAM_API_ID=
# NELA_TELEGRAM_API_HASH=
