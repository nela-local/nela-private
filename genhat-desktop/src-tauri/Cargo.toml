[package]
name = "app"
version = "0.1.1"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"
default-run = "app"

# Workspace root (doc_graph FastEmbed lives in-process; file-indexer sidecar builds separately).
[workspace]
members = ["."]
resolver = "2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.4", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
tauri = { version = "2.10.0", features = [] }
tauri-plugin-log = "2"
tauri-plugin-dialog = "2"
tauri-plugin-notification = "2"
notify-rust = "4"
tokio = { version = "1", features = ["full", "macros", "rt-multi-thread"] }
async-trait = "0.1"
toml = "0.8"
dotenvy = "0.15"
reqwest = { version = "0.12", features = ["json", "stream", "cookies"] }
futures-util = "0.3"
portpicker = "0.1"
uuid = { version = "1", features = ["v4"] }
dashmap = "6"
crossbeam-channel = "0.5"
base64 = "0.22"
sysinfo = "0.30"
rayon = "1.10"
wide = "0.7"

# ── RAG pipeline ──
rusqlite = { version = "0.32", features = ["bundled", "blob"] }
r2d2 = "0.8"
r2d2_sqlite = "0.25"
tantivy = "0.22"
pdf-extract = "0.12"
pdfium-render = { version = "0.8", features = ["image", "thread_safe"] }
image = "0.25"
docx-rs = "0.4"
zip = "2"
tar = "0.4"
flate2 = "1.1"
xml-rs = "0.8"
tempfile = "3"
sha2 = "0.10"
ed25519-dalek = "2.1"
hex = "0.4"
csv = "1"
calamine = "0.26"
walkdir = "2"
ignore = "0.4"
notify = "6.1.1"

# ── Doc-graph engine (structural KG + hybrid retrieval) ──
petgraph = { version = "0.6", features = ["serde-1"] }
bincode = "1.3"
thiserror = "1.0"
parking_lot = "0.12"
lopdf = "0.32"
quick-xml = "0.36"
tracing = "0.1"
fastembed = "5.17.3"

# ── ONNX Runtime (in-process DistilBERT classifier + KittenTTS + Parakeet ASR + FastEmbed) ──
ort = { version = "2.0.0-rc.11", features = ["download-binaries"] }
tokenizers = { version = "0.22", default-features = false, features = ["onig"] }

# ── Parakeet ASR audio preprocessing ──
hound = "3"
realfft = "3"
symphonia = { version = "0.5", features = ["mp3", "aac", "flac", "ogg", "pcm", "wav"] }

# ── Native microphone recording (WebView doesn't expose navigator.mediaDevices) ──
cpal = "0.15"

# ── KittenTTS text preprocessing ──
regex = "1"
fancy-regex = "0.14"
scraper = "0.22"
urlencoding = "2"

# ── Telegram user connector (MTProto; session stays on-device) ──
grammers-client = "0.7"

# ── Playground pipeline engine ──
imap = "2"
native-tls = "0.2"
tokio-cron-scheduler = "0.13"
handlebars = "6"
chrono = { version = "0.4", features = ["serde"] }
tauri-plugin-opener = "2"
keyring = "3"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(target_os = "macos")'.dependencies]
ort = { version = "2.0.0-rc.11", features = ["download-binaries", "coreml"] }

# ── MCP sidecar: Excel artifact generator ──
# Listed as a separate binary so `cargo build --bin mcp-server-excel` produces
# the sidecar that McpCoordinator spawns via stdio JSON-RPC.
[[bin]]
name = "mcp-server-excel"
path = "src/bin/mcp_server_excel.rs"

[[bin]]
name = "mcp-server-presentation"
path = "src/bin/mcp_server_presentation.rs"

[[bin]]
name = "mcp-server-html"
path = "src/bin/mcp_server_html.rs"

[[bin]]
name = "doc-graph-bench"
path = "src/bin/doc_graph_bench.rs"

[dependencies.rust_xlsxwriter]
version = "0.95"
